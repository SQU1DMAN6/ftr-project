{
  "name": "inker3",
  "version": "3.0.0",
  "private": true,
  "description": "FtR Inker 3.0 — Daemon Manager for Collaborative Repository Sync",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "check": "node --check main.js && node --check preload.js && node --check renderer/app.js"
  },
  "devDependencies": {
    "electron": "^41.5.0"
  }
}
